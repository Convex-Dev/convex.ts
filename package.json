{
  "name": "convex-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "demo": "pnpm --filter @convex-world/demo-site dev",
    "dev": "pnpm --filter @convex-world/demo-site dev",
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@10.12.4",
  "devDependencies": {
    "@types/jest": "30.0.0",
    "jest": "^29.7.0",
    "ts-jest": "29.4.3"
  }
}